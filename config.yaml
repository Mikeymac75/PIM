name: "Mac's Inventory PIM"
version: "1.0"
slug: "pim_app"
description: "Local Home Inventory and Forecasting System"
arch:
  - aarch64
  - amd64
startup: application
boot: auto
options:
  users:
    - "admin:password"
  secret_key: "change_me_to_something_random"
schema:
  users:
    - str
  secret_key: str
ports:
  8080/tcp: 8080
map:
  - config:rw
  - share:rw
